a = [1, 2, 3]
b = a
b.append(4)
와 같이 코드를 구성하면, b = a에서 얕은 복사가 이루어져서
a도 [1, 2, 3, 4]와 같이 배열이 변경된다. 만약 b에 깊은 복사를 적용하고 싶다면,
b = a[:] 또는
b = a.copy()
와 같은 방법을 사용할 수 있다. 만약 2차원 이상의 배열에 깊은 복사를 적용하고 싶다면,
copy 라이브러리를 불러와서 deepcopy() 함수를 적용시키면 된다. 예시는 다음과 같다.
import copy
b = copy.deepcopy(a)